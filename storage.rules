rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // No uploads from client. Only allow read public files.
      allow read: if true;
      allow write: if false;
    }
  }
}
